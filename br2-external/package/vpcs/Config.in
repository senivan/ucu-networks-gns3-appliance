config BR2_PACKAGE_VPCS
	bool "VPCS"
	depends on BR2_USE_MMU
	depends on BR2_TOOLCHAIN_HAS_THREADS
	help
	  Virtual PC Simulator used by GNS3.

	  https://github.com/GNS3/vpcs

comment "VPCS needs a toolchain w/ threads"
	depends on BR2_USE_MMU
	depends on !BR2_TOOLCHAIN_HAS_THREADS
